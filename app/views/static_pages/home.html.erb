<% provide(:title, t("home.title")) %>
<div class="center jumbotron">
  <h1><%= t"app_name" %></h1>
  <p>
    <%= raw t("home.description", href: link_to(t("rails_tutorial"), Settings.link.railshome)) %>
  </p>
  <%= link_to t("sign_up"), signup_path, class: "btn btn-lg btn-primary" %>
</div>
